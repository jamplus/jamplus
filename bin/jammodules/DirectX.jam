DXSDK_DIR = $(DXSDK_DIR:J=$(SPACE)) ;

rule UseDirectX TARGET : OPTIONS
{
	TARGET = [ _retrieveActiveTargetName $(TARGET) ] ;

	IncludeDirectories $(TARGET) : "$(DXSDK_DIR)Include" ;

	if link in $(OPTIONS)
	{
		LinkDirectories $(TARGET) : "$(DXSDK_DIR)/Lib/x86" ;
	}
}
